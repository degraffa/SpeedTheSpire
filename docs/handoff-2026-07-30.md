# G7 pause handoff — 2026-07-30

This is a deliberate pause point, not a G7 completion record. The binding
status remains in [the Stage B ledger](stage-b-tasks.md): G7 is unchecked and
the `g7-s1-verified` tag must not be created yet.

## Repository state being handed off

The user directed the in-progress G7 correction bundle to be committed and
landed rather than left as an uncommitted campaign worktree. This is an
intentional exception to the normal one-task/one-completion-commit cadence.
The bundle contains the campaign-discovered engine and replay corrections,
their focused regressions, campaign-driver durability work, report-accounting
hardening, and the deferred-obligation discharges recorded in the ledger. It
does **not** check any G7 acceptance box.

The most important landed corrections include:

- observable/private latch fidelity for Lantern, Gambling Chip, and Red Skull;
- Neow/equip sequencing for Tiny House, Pandora's Box, Calling Bell, and the
  other choice-bearing boss relics;
- filtered hand/grid command mapping, the tenth combat hand slot, and
  reward/shop/chest overlay timing;
- Corruption and Infernal-Blade/Blood-for-Blood cost persistence, Armaments
  ordering, empty-discard oversized draws, and terminal Reaper healing;
- Gremlin Visage Weak timing, MonsterStartTurn/Juggernaut ordering, THORNS
  damage typing for Sadistic Nature, and Sacred Bark potency;
- Wheel of Change, Big Fish, Living Wall, Wing Boots, Secret Technique, and
  Emerald-key run-layer/replay cases;
- narrow accounting for the already-understood Entropic Brew, Smoke Bomb,
  Living Wall, and Looter shapes, without allowing unrelated differences to
  hide behind those dispositions.

Use the focused tests and exact provenance in the changed files and in
[`b52_exercise_triage.md`](../tools/oracle_bridge/driver/b52_exercise_triage.md);
do not reconstruct those mechanics from this summary.

## Campaign is safely paused

Campaign artifacts remain outside Git, as required:

```text
D:\STS_BG_Mod\_oracle_data\campaigns\
  g7_random_b153_20260729_300000_324999
```

Durable boundary at the pause:

- campaign `g7_random_b153_20260729_300000_324999`;
- driver `b1.5.3`, policy `random-legal`, policy seed `1234`;
- 8,354 closed seeds, zero open;
- last closed seed `STS308353`; next seed `STS308354`;
- terminal action sum 435,502;
- 36.31 GiB free on `D:`.

Before shutdown, the supervisor/worker and
pipeline → orchestrator → Java → driver ancestry were validated against the
campaign identity and singleton token without printing the token. The exact
tree was then stopped. No matching G7 process remained, and an exclusive
singleton-lock acquire/release succeeded. No raw JSONL, progress, replay, or
launch-log artifact was deleted.

Resume from the repository root with the same identity and **without**
`--fresh`:

```powershell
C:\Python39\python.exe tools\oracle_bridge\driver\campaign_pipeline.py run `
  --campaign-id g7_random_b153_20260729_300000_324999 `
  --seeds D:\STS_BG_Mod\_oracle_data\g7_random_25000_seeds.txt `
  --policy random-legal `
  --policy-seed 1234 `
  --campaign-timeout 172800
```

The pipeline must recognize the existing progress file and begin with
`STS308354`. Stop if it proposes a fresh campaign, changes the fork hash or
driver version, reports any open seed before launch, or fails to acquire the
singleton lock.

## What the campaign proves — and does not prove

The last fully reprocessed random prefix is:

```text
D:\STS_BG_Mod\_oracle_data\campaigns\
  g7_random_b153_20260729_300000_324999\prefix_replay\
  closed-005000-b153-fixes11
```

That exact prefix contains 5,000 terminal runs and 260,523 captured actions:
4,935 runs / 254,734 actions are clean after known race handling. The remaining
65 runs / 5,789 actions are entirely the already-recorded Looter gold-ordering
deviation. After excluding the narrow capture races, the strict race-free
total is 4,918 runs / 253,737 zero-diff actions, with no untriaged difference.
The separate final greedy cohort
`g7_greedy_b153_20260729_200000_200011` completed 12 runs / 1,963 actions; its
four non-clean runs are the same Looter-standing shape.

This is useful differential evidence, but it is **not exhaustive gameplay
coverage**:

- every one of the 5,000 random runs died; 4,879 (97.58 %) ended on floors
  1–7, and none reached an Act 1 boss reward;
- the 12-run greedy cohort produced the only two boss-reward outcomes, so the
  combined evidence has only 2 boss rewards in 5,012 runs;
- registry sightings covered 398/405 `game_id` rows. The unseen rows were
  `Void`, `The Bomb`, `Circlet`, `Odd Mushroom`, `Spirit Poop`,
  `NoteForYourself`, and `The Mushroom Lair`. Some are unreachable or
  artificial in S1/A20, while the event-choice and rare-colorless cases need
  intentional targeting.

Consequently, simply completing more uniform random seeds is valuable for the
literal G7 action-volume bar and early-Act-1 confidence, but it will not
efficiently exercise late-Act-1 decks, relic combinations, boss rewards, or
rare nested screens. It also says nothing about Act 2/3 mechanics that are
outside the current S1 scope.

Before claiming that the verification “effectively catches everything,” add a
coverage-driven cohort alongside the volume run:

1. Resume the existing random campaign and periodically reprocess a closed
   prefix until the ledger's literal oracle-action bar is met with zero
   untriaged differences.
2. Add survival-biased policies or selected full-run seeds that deliberately
   reach late Act 1 and all three bosses; require a meaningful floor-band and
   boss-reward distribution, not merely a larger seed count.
3. Target rare branch families explicitly: event accept/decline alternatives,
   shop/reward/chest nested screens, full hand/potion slots, low-HP revive and
   healing paths, relic/card/potion interactions, and the reachable unseen
   registry rows above.
4. Promote each newly exposed mismatch to a focused regression and rerun the
   exact prefix. Keep capture races and standing deviations narrowly
   field/phase constrained.
5. Only after that, rerun every G7 checklist command on the integrated tree,
   fill the ledger Log, check each acceptance box, update the current-state
   documentation, and create/push `g7-s1-verified`.

The current test bundle was green under debug and ASan/UBSan during
development, and the Python driver/report suites were green. Because this is a
large partial integration, the orchestrator must re-run debug, ASan, and
release after merging it with current `master`; the post-merge result, not
this sentence, is authoritative.
