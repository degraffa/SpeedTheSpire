# a20.yaml -- ascension modifier table (rules-as-data, design doc B §6).
#
# Entry shape (frozen, design doc B §6):
#   - id: <int>            numeric id -- append-only, NEVER renumbered
#     level: <int>         ascension level (1..20)
#     scope: run-setup | monster | economy | event
#     mechanic: "<short description>"
#     provenance: "<Class.method (File.java:line) -- read in full>"
#     notes: "<optional>"
#
# Numbers live here ONLY after the cited line has been read during that row's task
# (stage-a §11 rule). This domain generates no enum; codegen validates entries and
# reports the row count in the manifest only.
#
# ONE ROW PER ASCENSION LEVEL, and `id` == `level`. Design §6's table groups
# levels that share a mechanism family (A2/A3/A4, A7/A8/A9, A17/A18/A19); this
# file splits them so "one row per modifier" is literal and every §6 table row
# maps onto at least one row here. There will never be a level 21, so id == level
# stays append-only by construction.
#
# `notes` opens with the row's S1 STATUS, one of:
#   IMPLEMENTED (<where>)   -- live in the sim, with tier-2 tests
#   N/A-FOR-S1 (<reason>)   -- deliberately not implemented; the reason is frozen
#
# THE NEGATIVE FREEZE. Design §6's "negative results, frozen so nobody 'fixes'
# them in later reading" list is recorded here as `no-such-modifier` statements so
# that a later reader who "finds" one of these has to delete a written line rather
# than merely add code. Each carries the line that was read to establish it:
#
#   no-such-modifier: campfire heal -- RestOption.<init> (RestOption.java:25)
#     computes healAmt = (int)(maxHealth * 0.3f) with NO ascensionLevel branch
#     anywhere in the constructor; the only modifiers are the Night Terrors daily
#     mod and the endless-mode FullBelly blight.
#   no-such-modifier: potion-drop chance -- AbstractRoom.addPotionToRewards
#     (AbstractRoom.java:580-607) sets chance = 40 for elite/monster/event rooms,
#     adjusts it only by blizzardPotionMod (the +-10 ratchet at :604-606) and the
#     White Beast Statue override; no ascensionLevel appears in the method.
#   no-such-modifier: normal-combat gold -- AbstractRoom.update
#     (AbstractRoom.java:324) is treasureRng.random(10, 20), unbranched.
#   no-such-modifier: elite-combat gold -- AbstractRoom.update
#     (AbstractRoom.java:316) is treasureRng.random(25, 35), unbranched. Only the
#     BOSS payout has an ascension branch (that is row id 13).
#   no-such-modifier: card-reward RARITY -- AbstractDungeon.rollRarity
#     (AbstractDungeon.java:1597-1603) is cardRng.random(99) + cardBlizzRandomizer
#     handed to getCardRarity; getCardRarityFallback (:1606-1614) hard-codes
#     rareRate = 3 / 40. No ascensionLevel term on either path.
#
# Rows 12 and 20 are the two ascension levels that are themselves negatives in S1;
# they carry their own N/A-FOR-S1 reason below.

- id: 1
  level: 1
  scope: run-setup
  mechanic: "elite room quota x1.6 during map room-type assignment"
  provenance: "AbstractDungeon.generateRoomTypes (AbstractDungeon.java:571-573): ascensionLevel >= 1 -> eliteCount = Math.round(availableRoomCount * eliteRoomChance * 1.6f), else Math.round(availableRoomCount * eliteRoomChance). Act-1 eliteRoomChance = 0.08f (Exordium.java:99)."
  notes: "IMPLEMENTED (assign_room_types, map_rooms.hpp -- the ascension argument run_begin already passes through). Pinned by the elite-quota row test."

- id: 2
  level: 2
  scope: monster
  mechanic: "normal-monster damage and move parameters"
  provenance: "Per-monster constructor / getMove branches on ascensionLevel >= 2, e.g. Cultist.<init> (Cultist.java:66): ritualAmount = ascensionLevel >= 2 ? 4 : 3."
  notes: "IMPLEMENTED (per-monster, as the per-tier columns of registry/monsters.yaml -- design §4.2). No number lives in this row; each monster row cites its own branch."

- id: 3
  level: 3
  scope: monster
  mechanic: "elite-monster damage and move parameters"
  provenance: "Per-elite constructor branches on ascensionLevel >= 3, e.g. GremlinNob.<init> (GremlinNob.java:72-74): bashDmg = 8, rushDmg = 16 (else the lower pair)."
  notes: "IMPLEMENTED (per-monster columns in registry/monsters.yaml) for the elites that exist; elites not yet landed carry the branch with their own row."

- id: 4
  level: 4
  scope: monster
  mechanic: "boss damage and move parameters"
  # DELIBERATE, AND NOT A DOCUMENT CONFLICT -- do not "fix" the asymmetry with
  # registry/monsters.yaml (recorded resolution, integration-11; row discharged
  # 2026-07-28). The `tackleDmg = 10` below is LITERALLY ACCURATE at the cited
  # line: the A4 branch of SlimeBoss's constructor really does assign it. But
  # `tackleDmg` feeds `damage.get(0)`, and SlimeBoss NEVER READS `damage.get(0)`
  # -- both takeTurn arms read `damage.get(1)` (SlimeBoss.java:137 and :144),
  # the slamDmg slot. So `tackleDmg` is a DEAD DamageInfo: an A4 scaling the
  # game computes and never applies.
  # This row is the A20 LEDGER, whose job is to record what the ascension branch
  # does in the Java -- so it names both assignments, the dead one included.
  # `registry/monsters.yaml` is the BEHAVIOUR table, whose job is to record what
  # the monster does -- so it omits `tackleDmg`, and is right to. The two files
  # differ because they answer different questions; that is why this is a
  # comment and not an edit on either side.
  provenance: "Per-boss constructor branches on ascensionLevel >= 4, e.g. TheGuardian.<init> (TheGuardian.java:107-109): fierceBashDamage = 36, rollDamage = 10; SlimeBoss.<init> (SlimeBoss.java:94-96): tackleDmg = 10, slamDmg = 38 -- tackleDmg is damage.get(0), which SlimeBoss never reads (both takeTurn arms use damage.get(1), :137/:144), so that half of the A4 branch is dead in the game and is deliberately absent from monsters.yaml; see the comment above."
  notes: "IMPLEMENTED (per-monster columns in registry/monsters.yaml) for the Act-1 bosses that exist."

- id: 5
  level: 5
  scope: run-setup
  mechanic: "between-act heal is 75% of MISSING HP instead of a full heal"
  provenance: "AbstractDungeon.dungeonTransitionSetup (AbstractDungeon.java:2582-2586): ascensionLevel >= 5 -> player.heal(MathUtils.round((maxHealth - currentHealth) * 0.75f), false), else player.heal(maxHealth, false)."
  notes: "N/A-FOR-S1 at run setup: the same line runs on the floor-0 entry, but the character sheet arrives at full HP (80/80, Ironclad.java:114), so missing HP is 0 and BOTH branches are no-ops. It only bites as the post-boss between-act heal, which is Act 2 (out of S1 scope, design §1.1). Its POSITION is still load-bearing and is honoured: it runs BEFORE the A14/A6/A10 block, so it can never see the reduced max HP. Pinned by the run-setup sweep. S2 note (S2.04): the SAME line (AbstractDungeon.java:2582-2586, cited above) also runs at the Act 1->2 and Act 2->3 transitions, where missing HP is typically nonzero, so the branch stops being a no-op there and the between-act heal becomes live (s2-design.md §2.5). This row stays N/A-FOR-S1 (S1 is Act-1-only); the live behavior is engine work owned by S2.12 (act transition), landed with its own tier-2 coverage -- recorded here so the status is not casually flipped before S2.12 lands."

- id: 6
  level: 6
  scope: run-setup
  mechanic: "act starts at 90% of max HP"
  provenance: "AbstractDungeon.dungeonTransitionSetup (AbstractDungeon.java:2594-2596): ascensionLevel >= 6 -> currentHealth = MathUtils.round(maxHealth * 0.9f). MathUtils.round (MathUtils.java:233-235) is (int)((double)v + 16384.5) - 16384."
  notes: "IMPLEMENTED (run_begin). ORDER IS LOAD-BEARING: this runs AFTER the A14 max-HP loss inside the same floorNum <= 1 block, so at A14+ the 90% is taken of the ALREADY-REDUCED max. Ironclad: 90% of 75 = 67.5 -> 68, not 90% of 80 = 72. S2 note (S2.04), verified run-start-only: the A6/A10/A14 modifiers all live inside the SAME `floorNum <= 1 && dungeon instanceof Exordium` guard as the run-setup block itself (AbstractDungeon.java:2590), one level above the ascensionLevel checks cited above -- so none of the three recur at the Act 1->2 or Act 2->3 transition; only the A5 heal and the fixed-list/pity resets run every act. This S1 implementation is therefore already correct as run-start-only, and the Acts 2-3 negative is pinned by the tier-2 sweep."

- id: 7
  level: 7
  scope: monster
  mechanic: "normal-monster HP roll range"
  provenance: "Per-monster constructor branches on ascensionLevel >= 7, e.g. Cultist.<init> (Cultist.java:59-63): setHp(50, 56) instead of setHp(48, 54)."
  notes: "IMPLEMENTED (per-monster hp columns in registry/monsters.yaml)."

- id: 8
  level: 8
  scope: monster
  mechanic: "elite-monster HP roll range"
  provenance: "Per-elite constructor branches on ascensionLevel >= 8, e.g. GremlinNob.<init> (GremlinNob.java:67-71): setHp(85, 90) instead of setHp(82, 86)."
  notes: "IMPLEMENTED (per-monster hp columns in registry/monsters.yaml) for the elites that exist."

- id: 9
  level: 9
  scope: monster
  mechanic: "boss HP roll range"
  provenance: "Per-boss constructor branches on ascensionLevel >= 9, e.g. TheGuardian.<init> (TheGuardian.java:100-102): setHp(250) + dmgThreshold 35 instead of setHp(240) + 30; SlimeBoss.<init> (SlimeBoss.java:89-93): setHp(150) instead of setHp(140). Some bosses add a further >= 19 tier ahead of this one (TheGuardian.java:97-99 keeps 250 HP but raises dmgThreshold to 40)."
  notes: "IMPLEMENTED (per-monster hp columns in registry/monsters.yaml) for the Act-1 bosses that exist."

- id: 10
  level: 10
  scope: run-setup
  mechanic: "Ascender's Bane curse added to the master deck at run start"
  provenance: "AbstractDungeon.dungeonTransitionSetup (AbstractDungeon.java:2597-2600): ascensionLevel >= 10 -> player.masterDeck.addToTop(new AscendersBane()). CardGroup.addToTop (CardGroup.java:455-457) is group.add(c) -- an ArrayList APPEND (addToBottom, :459-461, is the group.add(0, c) one). AscendersBane.<init> (AscendersBane.java:23-26): cost -2, CURSE/SPECIAL, isEthereal."
  notes: "IMPLEMENTED (run_begin). POSITION IS LOAD-BEARING: AbstractDungeon.<init> calls dungeonTransitionSetup at AbstractDungeon.java:287 and p.initializeStarterDeck only at :295-296, so the master deck is EMPTY when the curse is appended and the starting 5 Strike / 4 Defend / 1 Bash (AbstractPlayer.initializeStarterDeck, AbstractPlayer.java:386-390, also addToTop) land AFTER it. Ascender's Bane is therefore master-deck index 0."

- id: 11
  level: 11
  scope: run-setup
  mechanic: "one fewer potion slot (3 -> 2)"
  provenance: "AbstractPlayer.<init> (AbstractPlayer.java:211-213): ascensionLevel >= 11 -> --potionSlots, over the field default potionSlots = 3 (AbstractPlayer.java:144); the slot list is then built to that count (:214-217)."
  notes: "IMPLEMENTED (potion_slot_count, potions.hpp; stored into RunState by run_begin). Applied EARLIEST of all run-setup modifiers: it is in the AbstractPlayer constructor, which runs before the AbstractDungeon constructor that calls dungeonTransitionSetup."

- id: 12
  level: 12
  scope: economy
  mechanic: "card rewards can arrive pre-upgraded"
  provenance: "Exordium.initializeLevelSpecificChances (Exordium.java:107): cardUpgradedChance = 0.0f for Act 1, so the A12 branch has nothing to scale in Exordium. TheCity.initializeLevelSpecificChances (TheCity.java:84): cardUpgradedChance = ascensionLevel >= 12 ? 0.125f : 0.25f. TheBeyond.initializeLevelSpecificChances (TheBeyond.java:81): cardUpgradedChance = ascensionLevel >= 12 ? 0.25f : 0.5f."
  notes: "N/A-FOR-S1 (frozen negative, design §6): A12 is a NO-OP in Act 1 because Exordium pins the upgrade chance to 0.0f. S1 is Act 1 only (design §1.1), so no A12 modifier may be added to the sim. no-such-modifier for S1. S2 note (S2.04): Acts 2-3 do NOT pin the chance to 0 -- TheCity and TheBeyond (cited above) instead HALVE their act's base chance at A12 (0.25->0.125 in Act 2, 0.5->0.25 in Act 3), the per-act 0.125/0.25 halving of s2-design.md §2.5. This row stays N/A-FOR-S1 (S1's base chance is fixed at 0.0f) until S2.12 lands the per-act constants; only then does A12 leave no-op status, and only for Acts 2-3."

- id: 13
  level: 13
  scope: economy
  mechanic: "boss combat gold x0.75"
  provenance: "AbstractRoom.update battle-over block (AbstractRoom.java:291-296): tmp = 100 + miscRng.random(-5, 5); ascensionLevel >= 13 -> addGoldToRewards(MathUtils.round(tmp * 0.75f)), else addGoldToRewards(tmp). The miscRng draw happens on BOTH branches, so the stream is unaffected by the level. The branch is keyed off `this instanceof MonsterRoomBoss` (AbstractRoom.java:286), not an act check, so it fires for every MonsterRoomBoss instance including the A20 double boss's synthetic second one (goToDoubleBoss, ProceedButton.java:210-220; MonsterRoomBoss.onPlayerEntry pops bossList, MonsterRoomBoss.java:27-36). Separately, the Mind Bloom event's own boss re-fight branches the SAME threshold independently for a fixed payout with no miscRng draw: MindBloom.java:73-77, ascensionLevel >= 13 -> addGoldToRewards(25), else addGoldToRewards(50)."
  notes: "N/A-FOR-S1 for now: combat REWARD ASSEMBLY is not modelled at the run layer (run_advance.hpp scope note) -- the reward screen exists and assembles nothing, so there is no gold payout to scale. The row records the verified number for the reward-assembly owner. NOTE the negative twin: normal and elite gold have no such branch (see the no-such-modifier block at the top of this file). S2 note (S2.04): in Acts 2-3 (incl. the double boss) each MonsterRoomBoss fight draws its OWN independent miscRng.random(-5, 5) and applies the same x0.75 rounding -- two draws, two payouts, at the double boss (cited above). Mind Bloom's fixed 25/50 payout is a separate ascensionLevel >= 13 branch with no miscRng draw at all, owned by S2.33; the standard boss-gold draw for Act-2/3 fights is owned by S2.24 (City bosses) and owned by S2.28 (Beyond bosses). This row stays N/A-FOR-S1 until reward assembly lands with each of those tasks."

- id: 14
  level: 14
  scope: run-setup
  mechanic: "-5 max HP at run start (character-specific amount)"
  provenance: "AbstractDungeon.dungeonTransitionSetup (AbstractDungeon.java:2591-2593): ascensionLevel >= 14 -> player.decreaseMaxHealth(player.getAscensionMaxHPLoss()). Ironclad.getAscensionMaxHPLoss (Ironclad.java:168-170) returns 5. AbstractCreature.decreaseMaxHealth (AbstractCreature.java:211-223) subtracts, floors maxHealth at 1, and CLAMPS currentHealth down to the new maxHealth."
  notes: "IMPLEMENTED (run_begin). Runs FIRST of the three floorNum <= 1 modifiers, ahead of A6 -- see row 6. The clamp in decreaseMaxHealth is why HP is 75 for one statement before A6 rewrites it."

- id: 15
  level: 15
  scope: event
  mechanic: "harsher event branches, per event"
  provenance: "Per-event branches on ascensionLevel >= 15, e.g. WomanInBlue.<init> (WomanInBlue.java:47-51) swaps the leave option's text for a MathUtils.ceil(maxHealth * 0.05f) HP cost, and WomanInBlue.buttonEffect case 3 (WomanInBlue.java:96-98) applies that HP_LOSS damage."
  notes: "IMPLEMENTED (partial): registry/events.yaml rows 1-6 carry explicit per-event A15 columns and their native bodies consume them (The Cleric purify cost 50->75; Dead Adventurer opening risk 25->35; Golden Idol damage 25%->35% and max-HP loss 8%->10%; World of Goop gold-loss roll 20..50->35..75). Big Fish and Golden Wing explicitly record no A15 change because their Java classes have no ascension branch. Rows 7-31 remain owned by B4.12-B4.13."

- id: 16
  level: 16
  scope: economy
  mechanic: "shop prices x1.1"
  provenance: "ShopScreen.init (ShopScreen.java:227-229): ascensionLevel >= 16 -> applyDiscount(1.1f, false) (a price MULTIPLIER despite the name), applied before The Courier's 0.8f."
  notes: "N/A-FOR-S1 for now: shops are not modelled (run_advance.hpp scope note -- a shop room parks at an unimplemented stall). Row records the verified number for the shop owner."

- id: 17
  level: 17
  scope: monster
  mechanic: "normal-monster special behavior"
  provenance: "Per-monster getMove/takeTurn branches on ascensionLevel >= 17, e.g. Cultist.takeTurn (Cultist.java:95-97): RitualPower(this, ritualAmount + 1, false) instead of the plain ritualAmount."
  notes: "IMPLEMENTED (per-monster, in each monster's own module + registry row)."

- id: 18
  level: 18
  scope: monster
  mechanic: "elite-monster special behavior"
  provenance: "Per-elite branches on ascensionLevel >= 18, e.g. GremlinNob.takeTurn (GremlinNob.java:92-95): AngerPower(this, 3) applied 3 instead of AngerPower(this, 2) applied 2."
  notes: "IMPLEMENTED (per-monster) for the elites that exist."

- id: 19
  level: 19
  scope: monster
  mechanic: "boss special behavior"
  provenance: "Per-boss branches on ascensionLevel >= 19, e.g. SlimeBoss.takeTurn case 4 (SlimeBoss.java:125-127): MakeTempCardInDiscardAction(new Slimed(), 5) instead of 3; TheGuardian.<init> (TheGuardian.java:97-99): dmgThreshold 40 instead of the >= 9 tier's 35."
  notes: "IMPLEMENTED (per-monster) for the Act-1 bosses that exist."

- id: 20
  level: 20
  scope: monster
  mechanic: "double boss on the final act"
  provenance: "ProceedButton.update (ProceedButton.java:100-104): ascensionLevel >= 20 && bossList.size() == 2 -> goToDoubleBoss(), gated on AbstractDungeon.id.equals(\"TheBeyond\"). AbstractMonster.onFinalBossVictoryLogic (AbstractMonster.java:1058-1060) reads the same pair."
  notes: "N/A-FOR-S1 (frozen negative, design §6): the double boss is Act-3-only (the TheBeyond gate above) and S1 is Act 1 (design §1.1). A20 adds NOTHING to an Act-1 run beyond the union of levels 1..19. no-such-modifier for S1. S2 note (S2.04): the double-boss route is engine work owned by S2.12 (act transition / boss-list plumbing, the `bossList[1]` conditioning surface of s2-design.md §4.4); the second boss fight's own gold draw is owned per boss batch (S2.24, S2.28 -- see row 13's S2 note). This row stays N/A-FOR-S1 (S1 is Act 1) until S2.12 lands; the union claim above (level 19 == level 20 for an Act-1 run) is unchanged by S2."
